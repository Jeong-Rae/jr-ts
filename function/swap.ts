/**
 * 두 값을 교환한다.
 * @param a 첫 번째 값
 * @param b 두 번째 값
 * @returns 교환된 두 값의 배열
 * 
 * @example
 * swap(1, 2); // -> [2, 1]
 */
export function swap(a: number, b: number): [number, number] {
  return [b, a];
}
